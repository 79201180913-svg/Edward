[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "edward"
version = "0.6.0"
description = "Edward trading platform for T-Invest with adaptive market analysis and controlled execution"
requires-python = ">=3.14,<3.15"
dependencies = [
    "keyring>=25,<26",
    "openpyxl>=3.1,<4",
]

[project.scripts]
edward = "edward.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
