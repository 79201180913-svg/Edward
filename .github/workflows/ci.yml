name: Edward CI

on:
  push:
    branches: ["main", "version-0.2", "version-0.3", "version-0.4", "version-0.6-impl", "version-0.7", "version-0.8-analysis", "version-0.8.1-multifactor", "version-0.8.2-fundamental-analysis"]
  pull_request:
    branches: ["main", "version-0.2", "version-0.3", "version-0.4", "version-0.6-impl", "version-0.7", "version-0.8-analysis", "version-0.8.1-multifactor", "version-0.8.2-fundamental-analysis"]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: pip

      - name: Install project
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest keyrings.alt

      - name: Run tests
        run: pytest -q
